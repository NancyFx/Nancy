language: ruby
rvm:
  - 1.9.3
install:
  - sudo apt-get install mono-devel mono-gmcs mono-vbnc
  - gem install rake
  - gem install albacore
script: rake mono
notifications:
  email:
    - nancy@nancyfx.org
gemfile: Gemfile
